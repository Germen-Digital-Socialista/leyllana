# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.python-version
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ── Node / frontend (if any tooling is added later) ──────────────────────────
node_modules/
npm-debug.log*
yarn-error.log*

# ── Models, corpus, vector DB (large; shipped via installer, never git) ──────
models/
backend/models/
backend/bin/
corpus/
db/
*.gguf

# ── Secrets & local config ───────────────────────────────────────────────────
.env
.env.*
config.json
*.key
secrets/
# Config real de la maquina (rutas al binario y al modelo). El template versionado
# es leyllana.example.toml.
leyllana.toml

# ── OS / editor ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Salidas de la instrumentacion de desarrollo (tools/), nunca versionadas.
mediciones/

# Local agent tooling, not leyllana source: Claude Code config, skills installed
# via the skills.sh CLI, and its lockfile. Project-scoped to this machine.
.claude/
.agents/
skills-lock.json
